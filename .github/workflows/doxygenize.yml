name: Doxygenize
# You may pin to the exact commit or the version.
# uses: langroodi/doxygenize@1ce0e71f15064dab515ac89f2eb41914e6ebe0f7
uses: langroodi/doxygenize@v1.6.1
with:
  # Doxygen configuration file path
  doxygenconf: # default is ./Doxyfile
  # Generated HTML documents output folder
  htmloutput: # default is ./doc/
  # GitHub Pages branch name
  ghpagesbranch: # default is gh-pages
  # GitHub Pages directory path
  ghpagesdir: # default is ./
  # Toggle dark mode CSS
  darkmode: # optional
  # The type of runner that the job will run on
  runs-on: ubuntu-latest

  # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v3

      # Runs a single command using the runners shell
      - name: Run a one-line script
        run: echo Hello, world!

      # Runs a set of commands using the runners shell
      - name: Run a multi-line script
        run: |
          echo Add other actions to build,
          echo test, and deploy your project.
